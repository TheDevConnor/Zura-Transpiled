const Person := struct {
    name: string;
    age: int;

    const print := fn(self: Person) void {
        dis("Name: " + name + "\nAge: " + age);
    };
};

const main := fn() int {
    have p: Person;

    p.name = "John";
    p.age = 30;

    p.print();

    return 0;
};
