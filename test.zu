const main := fn() int {
   # While loop
   have x: int = 0;
   loop(1 < 10) : (++x) {
       # Do nothing 
   }

   # For loop
   loop(i in 0..10) {
       # Do nothing
   }

   # ForEach loop
   # have arr: [5]int = {1, 2, 3, 4, 5};
   # loop(i in arr) {
   #     # Do nothing
   # }

    return 0;
};
