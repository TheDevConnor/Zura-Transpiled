const main := fn () int! {
  @command<"pwd">();
  @command<"uname">("-a");
  @command<"date">();
  @command<"whoami">();
  @command<"uptime">();
  @command<"df">("-h");
  @command<"free">("-h");
  @command<"ls">("-l", "-a");
  @command<"ps">("-aux");
  @command<"ifconfig">();
  @command<"netstat">("-tuln");
  return 0;
};