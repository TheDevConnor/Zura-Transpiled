const min := fn<T> (a: T, b: T) T {
  if (b < a) return b;
  return a;
};

const max := fn<T> (a: T, b: T) T {
  if (a < b) return b;
  return a;
};

const add := fn (a: int!, b: int!) int! {
  return a + "b";
};
