const fib := fn (n: int) int {
    have a: int = 0;
    have b: int = 1;

    if (n <= 1) return n;

    loop(i = 2; i <= n) : (i = i + 1) {
        have temp: int = a + b;
        a = b;
        b = temp;
    }

    return b;
};

const main := fn () int {
    return fib(10); # 55
};