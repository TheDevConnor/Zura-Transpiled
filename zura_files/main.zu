const main := fn () int {
   # for loops
   loop(i = 0; i <= 10) : (++i) {
      dis("Hello, World!\n");
   }

   # while loops
   have x: int = 0;
   loop(x < 10) : (++x) {
      dis("Goodbye, World!\n");
   }
   return 0; 
};
