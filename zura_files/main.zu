const test_with_retries := fn (action: fn () int, retries: int) int {
  loop (i = 0; i < retries) : (i++) {
    if (action() == 0) return 0;
  }
  return 1;
};

const test := fn () int {
  return 0;
};

const main := fn () int {

  test_with_retries(test(), 3);

  return 0;
};