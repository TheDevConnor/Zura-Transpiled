const main := fn () int {
  have n1: int = 0;
  have n2: int = 1;
  have final: int = 0;

  loop (count = 1; count < 10) : (count = count + 1) {
    final = n1 + n2;
    n1 = n2;
    n2 = final;
  }

  return final;
};