# TODO: Add in type checking for template functions and calls
@template <typename T>
const add := fn(a: T, b: T) T {
   return a + b;
};

const main := fn() int {
   dis("Using an interger add function -> ", add(1, 2)<int>);
   dis("Using a float add function -> ", add(1.1, 2.2)<float>);
   return 0;
};