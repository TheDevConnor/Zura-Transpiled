const main := fn () int {
    have name: [256]char = [0];
    have age: [256]char = [0]; # lol

    @input("What is your name? ", name, 256);
    @input("How old are you? ", age, 256);

    have ageInt: int = @cast<int>(age);

    @dis("hello, ", name, "! You are ", ageInt, " years old.");

    return 0;
};