const main := fn () int! {
  have x: int! = @getArgc();
  if (x < 2) {
    @outputln(1, "You did not pass any inputs into Zura.");
    return 1;
  }
  @outputln(1, "You passed in ", x, " inputs into Zura.");
  have y: *[]str = @getArgv();
  have i: int! = 0;
  loop (true) {
    
  }
  return 0;
};