@import "lexer/helper.zu";

const main := fn () int {
  have num: [6]int = [0, 1, 2, 3, 4, 100]; # 0 is the terminator
  have li: int = len_int(num);
  dis(li, " -> is the length of the array.\n");

  return 0;
};