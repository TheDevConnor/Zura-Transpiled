const Day := enum {
    Monday,
    Tuesday,
    Wednesday,
    Thursday,
    Friday,
    Saturday,
    Sunday,
};

const main := fn() int {
    have day: Day;

    day = Day.Tuesday;

    if (day == Day.Monday) dis("Today is Monday");
    else dis("Today is not Monday");

    return 0;
};