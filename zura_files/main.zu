const Timer := struct {
  time: int,

  init := fn (self: *Timer) void {
    self.time = 0;
  };

  print := fn (self: *Timer) void {
    @dis("Time: ", self.time, "\n");
  };
};

const main := fn () int {
  have t: Timer;
  t.init();

  t.print();

  return 0;
};