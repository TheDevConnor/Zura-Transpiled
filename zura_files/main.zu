const Inside := struct {
  x: int;
};

const Outside := struct {
  a: Inside;
};

const main := fn () int {
  have hehe: Inside = { 
    x: 42
  };
  have HAHA: Outside = {
    a: hehe
  };

  return 1;
};