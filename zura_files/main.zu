@template<typename T>
const Zura := struct {
  value: T;

  print := fn(self: *Zura) void {
    dis(self.value);
  };
};

@template<typename T>
const add := fn(a: U, b: U) U {
  return a + b;
};

const main := fn() int {
  have zura: <int>Zura;
  zura.value = 10;
  zura.print();

  have zura2: <str>Zura;
  zura2.value = "Hello, Zura!";
  zura2.print();

  have zura3: <bool>Zura;
  zura3.value = true;
  zura.print();

  return add(10, 20);
};