const HelpMe := struct {
  x: int;
  y: float;
};

const main := fn() int {
  have Test: HelpMe = {
    x: 5,
    y: 3.0
  };
  return @cast<int>(Test.y); # 3
};


# const main := fn() int {
#   @dis("x is ");
#   have x: float = 5.0;
#   have y: float = 3.0;
#   if (x > y) {
#     @dis("greater\n");
#   } else {
#     @dis("less\n");
#   }
#   # 👍
#   return 0;
# };