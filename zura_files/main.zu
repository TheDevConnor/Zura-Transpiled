const add := fn (a: int, b: int) int {
   have x: int = a + b;
   return if (x == 12) {
      7;
   } else {
      0;
   };
};

const main := fn () int {
   have res: int = add(7, 5);
   return res;
};