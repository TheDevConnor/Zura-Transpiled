const power := fn (base: int, exp: int) int {
  if (exp == 0) return 1;
  loop (i = 1; i < exp) : (i++) {
    base = base * base;
  }
  return base;
};

const main := fn () int {
  @dis("2^3 = ", power(2, 3)); # 8 
  return 0;
};