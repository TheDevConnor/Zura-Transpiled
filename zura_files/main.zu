const HEHEHEHA := struct {
  x: int;
  y: int;
};

const main := fn () int {
  have test: HEHEHEHA = {
    x: 1, # Declared at -16(%rbp)
    y: 2, # Declared at - 8(%rbp) !!! YIPEE
  };

  return test.y;
};