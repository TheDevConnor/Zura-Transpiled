const k:=enum{a,b,c,d,e};const t:=struct{z:k,v:str};const main:=fn()int!{have l:[1]t=[{z:k.a,v:"3.14159"}];@output(1,l[0].v);return @cast<int!>(l[0].z);};
# This test is the only one that breaks. I need to change the offset of the struct in the variable table it seems