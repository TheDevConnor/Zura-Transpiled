const pow := fn (x: float, n: int) float {
    have res: float = 1.0;
    loop(i = 0; i < n) : (i = i + 1) {
        res = res * x;
    }
    return res;
};

const main := fn() int {
    have res: float = pow(2.0, 3);
    return @cast<int>(res);
};