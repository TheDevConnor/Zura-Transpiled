const Test := struct {
    a: int!,
};

const main := fn () int! {
    have test: *Test = @cast<*Test>(@alloc(@sizeof(Test)));

    test.a = 42;
    @outputln(1, "Test a: ", test&.a);

    return 0; 
};